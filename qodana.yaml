version: "1.0"

linter: jetbrains/qodana-jvm:2024.3

profile:
  path: .qodana/long-method-thresholds.xml

exclude:
  - name: All
    paths:
      - "**/*Test*.java"
      - "**/test/**"
      - "**/tests/**"
      
